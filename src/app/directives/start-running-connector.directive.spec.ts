import { StartRunningConnectorDirective } from './start-running-connector.directive';

describe('StartRunningConnectorDirective', () => {
  it('should create an instance', () => {
    const directive = new StartRunningConnectorDirective();
    expect(directive).toBeTruthy();
  });
});
